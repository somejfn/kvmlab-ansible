version: 1
config:
   - type: physical
     name: eth0
     subnets:
        - type: static
          address: "{{ item['ip'] }}"
          netmask: "{{ item['netmask'] }}"
          gateway: "{{ item['gateway'] }}"
